[build]
builder = "NIXPACKS"
buildCommand = "npm run build"

[deploy]
startCommand = "npm start"
healthcheckPath = "/"

[source]
directory = "apps/web"

[build.env]
NIXPACKS_NODE_VERSION = "18"

[environments.*.variables]
NODE_ENV = "production"